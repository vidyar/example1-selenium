language: python

python:
   - 2.7

addons:
  firefox: "23.0"

services:
  - selenium
#install: java -jar selenium-server-standalone-2.40.0.jar

before_script:
  - "export DISPLAY=:99.0"
  - "/etc/init.d/xvfb start"

script: python test_python_org_search.py

after_script:
  - "/etc/init.d/xvfb stop"
